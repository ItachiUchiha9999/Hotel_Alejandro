# Copiá este archivo a .env y completá los valores.
# El .env real NO se versiona (está en .gitignore).

DATABASE_URL="postgresql://postgres:enmali99@localhost:5432/sistema_hotelero_db?schema=public"

PORT=4000
NODE_ENV=development

# Orígenes permitidos para CORS, separados por coma.
CORS_ORIGINS=http://localhost:3000

# Empleado al que se atribuyen los movimientos mientras no exista login (STK-06).
DEFAULT_EMPLOYEE_ID=1
